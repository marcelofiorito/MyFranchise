specVersion: "3.0"
metadata:
  name: sac-overview
type: application
builder:
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateBundle
      configuration:
        archiveName: sac-overview
        additionalFiles:
          - xs-app.json
