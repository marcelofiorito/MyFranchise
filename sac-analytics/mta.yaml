_schema-version: "3.2"
ID: myfranchise-sac-analytics
version: 1.0.0
description: RunMyFranchise SAC Analytics — Calculation Views para SAC Live Connection

modules:
  - name: myfranchise-sac-analytics-db
    type: hdb
    path: db
    requires:
      - name: myfranchise-db
    build-parameters:
      ignore: ["node_modules/", ".env"]

resources:
  - name: myfranchise-db
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: myfranchise-db
